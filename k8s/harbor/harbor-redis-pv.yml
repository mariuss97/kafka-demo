apiVersion: v1
kind: PersistentVolume
metadata:
  name: harbor-redis-pv
  namespace: harbor-system
spec:
  accessModes:
    - ReadWriteOnce
  capacity:
    storage: 1Gi
  volumeMode: Filesystem
  local:
    path: /home/vagrant/data/pv/harbor-redis-pv
  nodeAffinity:
    required:
      nodeSelectorTerms:
      - matchExpressions:
        - key: kubernetes.io/hostname
          operator: In
          values:
          - master